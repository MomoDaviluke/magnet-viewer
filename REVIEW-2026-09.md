# 项目评审报告（2026-09-08）

> 审查对象：`magnet-viewer`（磁力链实时解析查看器 + 边下边播）
> 代码规模：业务代码 6555 行（core 13 模块 + ui 9 模块），测试 16 套
> 环境：Python 3.13.12 + libtorrent 2.1.1 + PySide6 6.11.2（Essentials + Addons，Windows）
> 与 2026-09-03 `REVIEW.md` 的关系：那份报告产出后项目经历了 fetcher 大重构
> （1600 行 → Facade 556 行 + 6 个单一职责模块）、设置接线、打包链路落地，
> 旧报告的整改清单已大面积过时（见 P2-1），本报告聚焦**当前状态**。
>
> **整改状态（2026-09-08 当日）**：P0-1 ~ P0-4 已全部修复并经实证复验
> + 16 套回归全绿；P1/P2 待排期。

---

## 一、审查方法

三路并行只读审计（core / ui / 工程化与安全）+ 主审交叉验证。所有 P0 结论**必须经实证**，
未经实证的一律标注「推测」，子代理结论与主审实证冲突时**以实证为准**（本次有 2 条被证伪/降级，见下）。

实证工具：offscreen Qt 实例化、真实 HTTP Range 请求、AST 扫描、全量覆盖率（`coverage_run.py full`）。

---

## 二、总体评估

| 维度 | 评级 | 说明 |
|---|---|---|
| 架构分层 | **良好** | 重构后循环依赖彻底打破（无子模块 import fetcher）；单锁归属（仅 `registry.lock`，property 同源）贯彻到位 |
| 核心机制 | **扎实** | 点播+等待、moov 尾部优先、分块位图快照——边下边播的设计与实现仍是项目最有价值的部分 |
| 测试体系 | **中等，但有结构性漏洞** | 16 套覆盖 core 良好（session 95% / taskstore 92% / resolver 88%），但 **CI 会把「全部 SKIP」判为绿灯**，且 UI 层覆盖极低 |
| UI 层质量 | **薄弱** | 本次 3 个 P0 中 2 个在 UI，且都落在覆盖盲区（add_download_dialog 仅 18%） |
| 可观测性 | 良好 | 日志设施完整，核心链路零静默（较 9-03 的 31 处静默大幅改善） |
| 打包与分发 | **良好** | 一键 build + 产物自检 + 103MB（较首版 -30%），多媒体后端已闭环 |
| 文档 | **需同步** | REVIEW.md 整改清单大面积过时 |

**结论**：core 层重构质量高、架构健康；问题集中在 **UI 层（未覆盖 → 真缺陷藏匿）** 与
**测试体系的判定逻辑（假绿）**。前者是功能问题，后者是让功能问题长期不被发现的系统性原因。

---

## 三、缺陷清单

### P0 — 功能失效 / 系统性漏检（建议立即修）

| # | 问题 | 证据 | 实证 |
|---|---|---|---|
| P0-1 | **「添加下载」100% 失败**：`priority()` 方法被同名 `QSpinBox` 实例属性遮蔽 | `ui/add_download_dialog.py:54` `self.priority = QSpinBox()` 遮蔽 `:86 def priority(self)`；调用点 `ui/main_window.py:327` `priority=dlg.priority()` | ✅ 实证：`TypeError: 'QSpinBox' object is not callable`。全项目 AST 扫描（排除 @property）仅此一处同类冲突 |
| P0-2 | **下载页每 700ms 全量重建**，选中/详情/右键菜单实际不可用 | `ui/downloads_pane.py:184` `self._model.removeRows(0, rowCount())` + 重建全部行；调用点 `ui/main_window.py:630`（状态定时器 `:138-139` 700ms） | ✅ 实证：选中 task-A → `selected_task()` 返回 `task-A`；调一次 `set_tasks()` 后 → `None`，详情区退回默认文案 |
| P0-3 | **CI 结构性假绿**：全 SKIP 也算通过，且导入错误被翻译成 SKIP | `regression_run.py:77-87` 仅 `rc==1` 计失败，`rc==2` 一律放行并打印「回归全绿」；`session_test.py:37-48` 等 7 套用 `except Exception` 包住整个 import 段 → `sys.exit(2)` | ✅ 实证：`from core.fetcher import NOT_A_REAL_NAME` → 退出码 2（SKIP），CI 放行。即重构漏删导出 / 模块语法错误都不会让 CI 变红 |
| P0-4 | **可用性不可判定时吐零数据**，违背本模块自身的 P1-8 契约 | `core/stream_server.py:108` `return logical, None, None, True` + `:110-116` `_range_available(pm=None, avail=None)` → `True` | ✅ 实证：`pieces_cb` 返回 `None`（不可判定）→ HTTP **206 + 1024 字节全零**。（注：子代理报的「pieces_cb 抛异常」路径实证返回 **503 正常**，已证伪；真正的漏洞是返回 None 这条兜底路径） |

### P1 — 明确隐患

| # | 问题 | 证据 | 状态 |
|---|---|---|---|
| P1-1 | 流服务无并发上限：播放器/扫描器可打满线程 | `core/stream_server.py:288` `_QuietHTTPServer(ThreadingHTTPServer)`，仅 `:330 daemon_threads=True`，无最大线程数、无请求超时 | 代码审读（历史债 P2-5，仍未修） |
| P1-2 | UI 层覆盖盲区，且缺陷正藏于此 | 全量集：`ui/main_window.py` 35%、`ui/add_download_dialog.py` **18%**、`downloads_pane` 25%；P0-1/P0-2 均落在这三个文件 | 实测（`coverage_run.py full`，TOTAL 73%） |
| P1-3 | 代理账号密码明文持久化 | `core/config.py:15-16` `proxy_user/proxy_pass`，`:50` `QSettings("Bitseed","MagnetViewer")` → Windows 注册表明文 | **✅ 已修复（2026-09-08）**：新增 `core/secretbox.py`（DPAPI，`dpapi:v1:` 前缀），`config.get/set` 对 `proxy_pass` 透明加解密；旧明文兼容直读；smoke_test [3g2] 防回归 |
| P1-4 | 持锁执行 I/O，阻塞告警线程 | `core/taskops.py` 的 `_convert_to_download_locked/resume_task/pause_task/remove_task`、`core/resolver.py:begin_resolve` 在 `reg.lock` 内做 libtorrent / 文件 I/O | 子代理审读，未实测。**⚠️ 需专项设计**：出锁涉及并发语义重构（快照-出锁-校验模式），不宜与其他修复混合，建议单独一轮处理 |
| P1-5 | 告警线程无锁读注册表 | `core/session.py:handle_alert` 无锁读 `current_record()`，与 UI 线程持锁写构成竞争 | 代码注释已声明 best-effort，建议显式标注或改快照 |

### P2 — 改进建议

| # | 问题 | 说明 |
|---|---|---|
| P2-1 | **`REVIEW.md` 整改清单大面积过时** | `:277` 标「P2-2 缓存上限 ❌ 未实现」——实际已实现（`core/cache_quota.py` + 设置接线）；`:283` 测试矩阵「❌ 未做」——实际 `hybrid_v2_test` 19 项已完成；`:286` 覆盖率「❌ 未接入」——实际 `coverage_run.py` 已接入；`:287` 打包与依赖锁定「❌ 未做」——实际已落地。**✅ 已缓解（2026-09-08）**：旧报告顶部已加过时横幅指向本报告 |
| P2-2 | CI step 名过时 | `.github/workflows/ci.yml` 中 `name: Run 8-suite regression`，实际已是 16 套。**✅ 已修复（2026-09-08）**：改为 `Run 16-suite regression`，且安装改用 `requirements.lock` |
| P2-3 | `ui/main_window.py` 691 行过载 | 混杂解析编排、预览播放、下载管理、设置、状态轮询五类职责，建议按页签拆出 controller |
| P2-4 | 预览缓存明文落系统临时目录 | 同机其他进程可读（设计取舍，建议在 README 明示） |
| P2-5 | ~~`persist.restore_task` 对 resume dict 做属性赋值~~ | **已证伪（2026-09-08 实测）**：libtorrent 2.1.1 的 `lt.read_resume_data()` 返回的就是 `add_torrent_params` 对象，属性赋值合法；persist_test「启动恢复九组」覆盖该链路 |

### 已证伪 / 降级（不列入缺陷）

- ~~`core/registry.py:153` `resolving` 被遮蔽~~ → 实为 `@property` + setter，正常用法（首版 AST 扫描误报，已修正扫描器）
- ~~`ui/file_tree.py` `select_file` O(n²) 致大种子卡死~~ → 实测调用点唯一（`main_window.py:429`，解析完成后选中首个可预览文件），单次 O(n)，非每帧触发，降为观察项

---

## 四、覆盖率与盲区（全量集，TOTAL 73%）

| 模块 | 覆盖 | 评价 |
|---|---|---|
| `core/session.py` `core/taskstore.py` `core/resume.py` | 92–95% | 健康 |
| `core/resolver.py` `core/stream_server.py` | 86–88% | 健康 |
| `core/scheduler.py` | 66% | 可接受（快速集仅 25%，必须跑 full 才点亮） |
| `ui/preview_pane.py` `ui/settings_dialog.py` | 72–78% | 可接受 |
| `ui/main_window.py` | **35%** | 盲区最大（265 行未覆盖）——且是最大文件 |
| `ui/add_download_dialog.py` | **18%** | 盲区冠军（48/60 行未覆盖）——P0-1 就在这里 |
| `ui/downloads_pane.py` | 25% | P0-2 在这里 |

---

## 五、整改路线建议

1. **第一阶段（P0，半天）**：P0-1 改名（如 `priority_spin` + `priority_value()`）并补一条冒烟断言；
   P0-2 改增量刷新（按 info_hash diff 更新行，保留选中与滚动位置）；
   P0-4 把「不可判定」当不可用（`_availability` 兜底返回 `ok=False`，`_range_available` 收到 `pm=None and avail=None` 时返回 False → 503）；
   P0-3 把 `regression_run.py` 的判定改为「全 SKIP = 失败」，且专项测试的 import 段只捕 `ImportError`（语法错误/逻辑错误必须冒泡为失败）。
2. **第二阶段（P1）**：P0-1/P0-2 的回归断言补齐（钉死「添加下载能提交」「刷新后选中保持」）；
   流服务加最大线程数与超时；代理凭据至少做一次混淆。
3. **第三阶段（P2）**：REVIEW.md 整改清单同步；main_window 拆分；CI step 改名。

---

*本报告所有标注「✅ 实证」的结论均附可复现的最小用例；标注「推测」或「子代理审读，未实测」的部分尚未经实证。*
