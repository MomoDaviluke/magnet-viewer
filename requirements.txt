# 运行时依赖
# 已验证环境：Python 3.13.12 + PySide6 6.11.2（Essentials + Addons）
#             + libtorrent 2.1.1（Windows）
# 注意：libtorrent 2.0 与 2.1 的会话配置 API 不兼容（2.1 移除 settings_pack），
#       升级时请以 2.1 的 lt.session(dict) 写法为准（本项目已适配）。
# 注意：PySide6_Addons **不是可选装饰**——Qt 多媒体后端插件
#       （plugins/multimedia/ffmpegmediaplugin.dll 与 avcodec/avformat/avutil）
#       只在这个包里发。只装 Essentials 时 QMediaPlayer 实例化即
#       ResourceError 'Not available'，双击视频能起流但播不出来（2026-09-08 实测）。
PySide6_Essentials>=6.11
PySide6_Addons>=6.11
libtorrent>=2.0.10

# 测试可选依赖（moov_stream_test.py / qt_stream_open_test.py 需要 ffmpeg 二进制）：
#   pip install imageio-ffmpeg
# 缺失时两个测试以退出码 2（SKIP）显式跳过，不会假装通过。